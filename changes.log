2026-04-12 | Backend: oversized assignment warning + override contract
- Added oversized-table warning threshold (4+ extra seats) in booking assignment validation.
- Required `confirm_oversized=true` for oversized table assignment/create/update flows.
- Added structured warning payload and availability response buckets: `recommended_tables` and `larger_tables`.

2026-04-12 | Tests: booking oversized contract coverage
- Expanded `tests/Feature/BookingsApiTest.php` for oversized-warning rejection and confirmed override success.
- Added availability response assertions for recommendation buckets and warning threshold metadata.

2026-04-12 | Frontend: admin bookings board
- Added `frontend/src/pages/admin/AdminBookingsPage.jsx` with status/date filters, assignment assist, and oversized confirmation modal.
- Added API wrappers `frontend/src/lib/bookings.js` and `frontend/src/lib/tables.js`.
- Wired `/admin/bookings` route and navigation entry.

2026-04-12 | Frontend: admin booking detail page
- Added `frontend/src/pages/admin/AdminBookingDetailPage.jsx` for editing booking fields, status, notes, cancel action, and assignment assist.
- Added route `/admin/bookings/:id` and "Details" links from bookings board.
- Added frontend tests in `frontend/src/pages/admin/AdminBookingDetailPage.test.jsx`.

2026-04-12 | UI/API alignment: status transition guards
- Added client-side status transition guard in booking detail status selector.
- Mirrored transition rules in backend validation (`BookingsController`) for update/cancel endpoints.
- Added feature tests for invalid transition rejection and terminal-state cancel rejection.

2026-04-12 | Docs: status transitions + changelog workflow
- Added a README status-transition reference for staff/dev with explicit allowed/terminal booking state changes.
- Added AGENTS guidance to keep `changes.log` append-only and updated after each completed implementation task.

2026-04-12 | Frontend: booking creation modal on admin board
- Added inline "New booking" modal to `AdminBookingsPage` with staff fields for guest, party size, timing, status, optional table, and notes.
- Added create flow in `frontend/src/lib/bookings.js` (`createBooking`) and board refresh/auto-select after successful creation.
- Added oversized confirmation retry path for create requests and Vitest coverage for create modal flow.

2026-04-12 | Frontend: create modal time-range guard
- Added client-side validation in `AdminBookingsPage` to block submission when booking end is not after booking start.
- Added Vitest coverage for invalid time-range submission in `frontend/src/pages/admin/AdminBookingsPage.test.jsx`.

2026-04-12 | Dev runtime fix: Vite base path alignment
- Updated `public/spa.php` to load dev scripts from `/spa` base (`/spa/@vite/client` and `/spa/src/main.jsx`) to match `vite.config.js`.
- Fixes MIME/CORS-style browser errors caused by Vite returning plain-text 404 for root-path module URLs.

2026-04-12 | Dev runtime fix: React refresh preamble injection
- Added `@react-refresh` preamble script to `public/spa.php` dev mode output.
- Fixes `@vitejs/plugin-react can't detect preamble` runtime error when serving HTML through PHP instead of Vite index transform.

2026-04-12 | Frontend: booking modal field spacing polish
- Added extra right-side input padding for text/email/number fields in admin forms to improve visual spacing in the New Booking modal.

2026-04-12 | Frontend: booking modal field spacing correction
- Reverted the prior padding-only tweak and fixed root cause by making admin form controls `width: 100%` with `box-sizing: border-box`.
- Prevents text/email/number inputs from overflowing into adjacent grid columns or crowding the modal edge.

2026-04-12 | Frontend: bookings board action column layout
- Stacked bookings action buttons vertically in the table actions cell to avoid horizontal overflow (`Assign table` + `Details`).

2026-04-12 | Frontend: bookings board column width + action button sizing fix
- Added bookings-table-specific override to remove inherited `min-width: 860px` that was forcing unnecessary horizontal scroll.
- Refined actions cell/button styles so stacked buttons are compact and center-aligned instead of stretching full-cell width.

2026-04-12 | Frontend auth polish: nav session state + login redirect
- Updated `NavBar` to detect authenticated session via `/api/auth/me` and show `Logout` instead of `Login`/`Sign up` when signed in.
- Added logout action in nav that clears session server-side and redirects to home.
- Updated login flow to redirect to `/` after successful sign-in.

2026-04-12 | Frontend bookings UX: amend label + auto-dismiss notifications
- Renamed bookings board row action from `Details` to `Amend`.
- Added 4-second auto-dismiss behavior for booking success/error feedback on board and detail pages (including create and availability errors).

2026-04-12 | Frontend fix: booking detail availability party size source
- Fixed initial `AdminBookingDetailPage` availability lookup to use the loaded booking form values (including party size) instead of stale default form state.
- Added test coverage to verify initial availability request uses the booking's actual `party_size`.

2026-04-12 | Phase 3 start: validation and friendly error consistency pass
- Added shared frontend helper `frontend/src/lib/errors.js` (`getFriendlyError`) to consistently extract first field error, payload message, or fallback text.
- Applied helper across auth, menu, and booking screens to normalize user-facing error feedback and avoid raw/inconsistent API message handling.
- Verified frontend and backend suites after changes (`npm run test:frontend`, `phpunit --testsuite Feature`).

2026-04-12 | Frontend UX polish: consistent error language tone
- Standardized fallback error phrasing across auth/menu/bookings to clear, action-oriented copy ("Could not ... Please ...").
- Updated one menu page test assertion to match revised copy while preserving behavior checks.

2026-04-12 | Frontend UX polish: consistent success message tone
- Standardized success copy across bookings/menu/auth actions to consistent "... successfully" phrasing.
- Updated booking board test assertion for revised create-success message and re-ran frontend test suite.

2026-04-12 | Phase 3: authorization hardening foundations
- Added user roles via migration (`users.role`) and auth role assignment policy (first account becomes `manager`, later signups default to `customer`), and exposed role in `/api/auth/me`.
- Enforced staff/manager authorization (`403 Staff access required`) for mutating menu/tables/bookings/uploads API endpoints.
- Updated frontend role handling (`AdminGuard`, `NavBar`) to gate admin UI by role, plus added feature tests for customer-role denial on menu/table/booking creation.

2026-04-12 | Phase 3: authorization negative test matrix expansion
- Added feature tests asserting customer-role denial (`403`) for mutating flows beyond create: menu update/delete/reorder, tables update/delete/reorder, and bookings update/cancel/assign.
- Added reusable customer-client helpers in menu/tables/bookings feature tests for role-specific authorization checks.
- Re-ran feature suite after updates (44 tests, 270 assertions passing).

2026-04-12 | Phase 3: booking audit trail foundation
- Added booking audit storage via migration `database/migrations/20260412_000530_create_booking_events_table.php` and model `app/models/BookingEvent.php`.
- Instrumented `BookingsController` to record lifecycle events for create, detail updates, status changes, cancel, and table assignment/reassignment/unassignment.
- Extended booking `show` response with `events` and added feature coverage validating expected lifecycle event types.

2026-04-12 | Frontend: booking detail activity panel
- Added an Activity panel to `AdminBookingDetailPage` that renders booking audit events returned by `GET /api/bookings/show/{id}`.
- Included readable event titles/details and timestamps plus supporting styles for event cards.
- Added frontend test coverage for event rendering and re-ran frontend + feature suites.

2026-04-12 | Ops: restore baseline app data after local reset
- Upserted `john@example.com` as `manager` in `phpsk.users` (created account if missing).
- Re-seeded menu via `cli/seed_menu.php` from `menuplan/Restaurant.md` (11 items, all available).
- Verified baseline counts in `phpsk`: users=1, menu_items=11, tables=15, bookings=0.

2026-04-12 | Frontend: bookings board selection usability fix
- Added automatic selection of the first booking when the board loads (or when current selection is no longer in filtered results).
- Made booking rows clickable so staff can select a booking directly from the table to drive Assignment Assist.

2026-04-12 | Auth stack: change password + navbar account hamburger
- Added backend endpoint `POST /api/auth/change_password` with CSRF/auth checks, current-password verification, strong validation, and session refresh after update.
- Added frontend change-password flow (`/change-password`) and API client helper in `frontend/src/lib/auth.js`.
- Updated `NavBar` to use a hamburger account dropdown: logged-out shows `Login`/`Sign up`; logged-in shows `Change password`/`Logout`.

2026-04-12 | UX polish: account menu icon sizing + password page flow
- Increased hamburger trigger size for clearer visibility and touch target in navbar.
- Removed automatic redirect after password change; page now stays in place with success feedback and explicit "Back to home" action.

2026-04-12 | UX polish: hamburger icon rendering + outside-click close
- Replaced text-based hamburger glyph with a custom 3-bar icon and fixed dimensions for consistent visible sizing.
- Added outside-click/touch handling so the account dropdown closes when clicking away.

2026-04-12 | Data: seeded initial dining table mix
- Added migration `database/migrations/20260412_000500_seed_dining_tables.php` to seed 15 tables (7 small, 5 medium, 3 large).
- Applied migration in dev DB and verified distribution counts: 7 (`<=2` seats), 5 (`4-5` seats), 3 (`6-10` seats).

2026-04-12 | Data: normalized table names by type
- Added migration `database/migrations/20260412_000510_normalize_table_type_names.php` to rename seeded tables to `Small Table XX`, `Medium Table XX`, and `Large Table XX`.
- Standardized seats per type to reduce confusion: small=2, medium=4, large=8.

2026-04-12 | Frontend mobile + keyboard usability: admin bookings surfaces
- Improved bookings board mobile usability with intentional horizontal table scrolling, larger touch targets, and stronger focus-visible states for keyboard navigation.
- Added keyboard-accessible booking row selection (`tabIndex` + Enter/Space) and prevented action-button clicks from triggering row-selection side effects.
- Added modal usability improvements on booking board/detail: autofocus on open, Escape-to-close, and backdrop click-to-close where operations are not in-flight.

2026-04-12 | Frontend responsive UX: admin menu mobile actions and condensed columns
- Updated `AdminMenuPage` actions to icon + label controls and grouped visibility/edit/delete together in the Actions column for consistent mobile use.
- Added responsive behavior to hide `Up`/`Down` reorder buttons plus `Category` and `Price` columns on narrow screens while keeping full details accessible via edit view.
- Added mobile stacking/sizing styles for action controls and updated the admin menu reorder test for accessible move-button labels.

2026-04-12 | Frontend responsive polish: tighter menu dish column wrapping on mobile
- Added a dedicated `menu-table` class and mobile table layout rules to narrow the dish column while preserving readability.
- Enabled stronger dish-name/description wrapping (`overflow-wrap` + `word-break`) and reduced dish thumbnail size on small screens.
- Keeps long dish titles/descriptions usable without forcing extra horizontal scrolling.

2026-04-12 | Frontend responsive UX: hide menu status column + drag-to-scroll table on mobile
- Hid the menu Status column on narrow screens since the visibility action button already communicates current state.
- Added mobile drag-to-scroll behavior on the menu table wrapper so users can swipe/drag horizontally from table content, not just the scrollbar.
- Disabled row drag-reorder on narrow screens to avoid conflicts with horizontal drag-scrolling interactions.

2026-04-12 | Frontend responsive polish: extra-compact mobile order column in menu table
- Added a dedicated `menu-col-order` header class and hid the `Order` header label on narrow screens.
- Reduced mobile width/padding of the order column so the drag handle occupies minimal space.
- Preserved desktop labeling and reorder behavior while maximizing space for dish/action content on phones.

2026-04-12 | Frontend responsive refinement: aggressively compact mobile order column
- Tightened mobile order column width constraints to 24px (min/max locked) and reduced cell side padding.
- Centered order-cell content and removed extra control gap so only the drag handle consumes space.
- Shrunk drag-handle footprint for mobile while keeping it clickable and visible.

2026-04-12 | Frontend responsive tweak: compact mobile menu action buttons
- Reduced mobile actions-column footprint and switched stacked buttons from full-width stretch to compact left-aligned sizing.
- Added tighter width caps, padding, and font sizing for `Visible`, `Edit`, and `Delete` buttons on narrow screens.
- Keeps icon+label readability while reclaiming horizontal space for dish content.

2026-04-12 | Frontend responsive fix: remove excess mobile menu right overflow + button spacing
- Reduced mobile menu table minimum width and made the actions column content-sized to eliminate extra blank scroll area on the right.
- Scoped zero-gap order-control compaction to the order cell only (`menu-order-controls`) so action buttons are no longer stuck together.
- Added explicit vertical gap for stacked mobile action buttons.

2026-04-12 | Frontend responsive rebalance: mobile menu order/actions column sizing
- Rebalanced mobile menu table layout from rigid fixed widths to a tighter auto layout to prevent oversized order-column behavior.
- Kept order and actions columns constrained with nowrap behavior while returning right-cell padding for cleaner visual spacing.
- Increased stacked action-button vertical spacing slightly and retained compact button sizing.

2026-04-12 | Frontend mobile interaction fix: restore menu drag-reorder with swipe scrolling
- Re-enabled row `draggable` behavior on narrow screens (it had been disabled during swipe-scroll rollout).
- Updated table swipe-drag guard to skip drag-handle/order-control touches so reorder gestures are not intercepted by horizontal scrolling.
- Added `touch-action: none` to the mobile drag handle to improve pointer behavior during reorder gestures.

2026-04-12 | Frontend mobile fix: custom touch drag reorder for menu rows
- Added handle-based pointer reorder logic for narrow screens using `elementFromPoint` row detection and drop-target highlighting.
- Kept native HTML5 drag/drop for wider screens while using touch-friendly pointer drag on mobile.
- Preserved horizontal swipe-scroll for table content, with drag-handle touches now dedicated to reorder.

2026-04-12 | Frontend UX hint: mobile menu reorder discoverability
- Added a mobile-only helper line on Admin Menu: "Tip (mobile): drag the :: handle to reorder rows."
- Kept the existing desktop tip unchanged while showing the new hint only at narrow-screen breakpoints.

2026-04-12 | Frontend responsive nav: mobile hamburger now contains full navigation
- Updated `NavBar` so narrow screens collapse all primary links (Home/Menu/Admin links) into the existing hamburger dropdown.
- Kept widescreen navigation behavior unchanged while expanding mobile dropdown content to include both navigation and account actions.
- Added a dropdown divider and mobile dropdown width polish for clearer grouping and touch usability.
